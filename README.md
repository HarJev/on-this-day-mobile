# On This Day Mobile

Flutter mobile application for On This Day.

The initial app is a minimal, product-neutral shell. Product behavior is defined
in `docs/PRODUCT.md`, and implementation boundaries are defined in
`docs/ARCHITECTURE.md`.

For local setup and verification, see `DEVELOPMENT.md`.
